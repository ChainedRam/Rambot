using System;

namespace KLD.TeamSpeak.Core
{
    [Serializable]
    public class LoginSoundEvent //: Event
    {
       /* public LoginSoundEvent() : base("LoginSound", (int)EventType.Login)
        {
        }

        //TODO check if play exist 
        public override void Raise(string invoketId, IBot bot)
        {
            ((Bot)bot).loader.LoadCommand("play").Execute("!play -login", invoketId,  bot); 
        }*/
    }
}
